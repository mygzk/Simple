apply plugin: 'java-library'

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
}

sourceCompatibility = rootProject.ext.dependVersion["javaVersion"]
targetCompatibility = rootProject.ext.dependVersion["javaVersion"]
