<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>


        <item name="android:editTextStyle">@style/EditTextStyle</item>
    </style>

    <style  name="EditTextStyle" parent="android:Widget.EditText">
        <item name="android:textCursorDrawable">@mipmap/ic_launcher</item>
        <item name="android:textSelectHandle">@mipmap/ic_launcher</item>
    </style >


    <!--dialog tv title通用样式-->
    <style name="comm_dialog_title_style">
        <item name="android:gravity">center</item>
        <item name="android:height">@dimen/comm_height46</item>
        <item name="android:textColor">@color/front_4</item>
        <item name="android:textSize">@dimen/front_seize16</item>
    </style>

    <!--button通用样式-->
    <style name="comm_btn_style">
        <item name="android:gravity">center</item>
        <item name="android:height">@dimen/comm_height</item>
        <item name="android:textColor">@color/front_4</item>
        <item name="android:textSize">@dimen/front_seize16</item>
        <item name="android:background">@color/front_3</item>
    </style>
    <style name="Transparent" parent="Theme.AppCompat.Dialog">
        <item name="android:windowNoTitle">false</item>
        <item name="android:windowFullscreen">true</item>
        <!--设置dialog的背景-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--设置Dialog的windowFrame框为无-->
        <item name="android:windowFrame">@null</item>
        <!--设置无标题-->
        <item name="windowNoTitle">true</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否半透明-->
        <item name="android:windowIsTranslucent">true</item>
        <!--设置窗口内容不覆盖-->
        <item name="android:windowContentOverlay">@null</item>
        <!--设置动画，在这里使用让它继承系统的Animation.Dialog-->
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <!--背景是否模糊显示-->
        <item name="android:backgroundDimEnabled">true</item>


    </style>

</resources>
